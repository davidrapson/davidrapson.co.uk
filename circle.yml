general:
  artifacts:
  - "build"
dependencies:
  override:
    - npm install
    - bundle install
test:
  override:
    - gulp build
deployment:
  production:
    branch: master
    commands:
    - gulp build
