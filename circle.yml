machine:
  ruby:
    version: 2.2.2
dependencies:
  override:
    - npm install -g gulp
    - npm install
    - bundle install
test:
  override:
    - gulp build
deployment:
  production:
    branch: master
    commands:
    - gulp deploy
