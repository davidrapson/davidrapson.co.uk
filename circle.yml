machine:
  ruby:
    version: 2.2.2
  node:
    version: 4.1.0
dependencies:
  override:
    - npm install
    - bundle install
deployment:
  production:
    branch: master
    commands:
    - npm run deploy
